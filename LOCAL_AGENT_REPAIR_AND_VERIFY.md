You are the operational executor for the SynapseDoc repository. Work directly in the real local workspace after the repair ZIP has been extracted at the repository root. Read AGENTS.md and use Graft first. Do not redesign the application. Preserve all unrelated work and .env.local, and never print or commit secrets.

Objective: verify the supplied deterministic Vitest and Webpack-dev corrections, then finish every item that requires the real Windows workspace, installed dependency tree, browser, credentials, or external services.

1. Run `git status --short` and inspect the ZIP changes to package.json and vitest.config.ts. Stop rather than overwrite unrelated edits. The intended changes are: pin Vitest to 4.1.9, run unit tests in one fork with file parallelism disabled, make `npm run dev` use Webpack to avoid the observed Turbopack os error 8 panic, and retain `npm run dev:turbo` as an explicit diagnostic option.
2. Ensure the shell uses the canonical drive-letter casing for the repository path. Record `node --version`, `npm --version`, `npx vitest --version`, and `pwd` or PowerShell equivalent. Prefer the project's existing supported Node installation; if the runner-state error persists on Node 24, repeat only the failing test gate on Node 22 LTS and report the comparison. Do not hide or rewrite failures.
3. Remove only generated dependency/build state, not source or secrets: delete node_modules, .next, and any Vitest/Vite caches using the local environment's safe commands. Then run `npm install` so the exact Vitest pin is installed. Do not use `npm audit fix --force`.
4. Run, in order: `npm run format:check`, `npm run type-check`, `npm run lint`, `npm run test`, `npm run build`. If a gate fails, capture the complete first actionable stack trace, make the smallest evidenced correction, and rerun that gate plus downstream gates. Confirm that tests actually execute assertions, not merely collect files.
5. If the Vitest runner still fails, run one diagnostic pass with `npx vitest run --pool=forks --maxWorkers=1 --no-file-parallelism --reporter=verbose`. Check for multiple installed Vitest copies with `npm ls vitest vite @vitejs/plugin-react`; check canonical path casing and antivirus/EDR interference; do not disable tests or weaken assertions. Return raw evidence if unresolved.
6. Run `npm audit --json` and preserve the report as an uncommitted local artifact. For each moderate/high/critical finding, identify the exact dependency path, whether it is production or development-only, whether a non-breaking patched version exists, and whether the vulnerable code is reachable in SynapseDoc. Apply only safe, non-breaking upgrades supported by the audit and package release data, then rerun install, tests, type-check, lint, and build. Do not claim all vulnerabilities fixed if any remain, and do not use force upgrades without a separate impact analysis.
7. Verify install-script status for core-js, sharp, and unrs-resolver from the actual package manager output. If scripts were blocked, determine which package manager or security policy blocked them. Approve or rebuild only the exact trusted packages required by the dependency tree, then rerun build and image/PDF paths that depend on them. Do not globally disable script protections.
8. Start the app with `npm run dev`. Confirm logs show Webpack rather than Turbopack and that the server remains alive. Do not use `npm run dev:turbo` for acceptance. If Webpack also exhausts memory, capture process memory and Windows commit-limit evidence before changing settings. Delete the generated .local-dev.log after preserving only redacted diagnostics in the report.
9. Execute the authenticated visual/runtime matrix at 360, 768, 1280, and 1536 px in light and dark themes. Exercise documents, document detail, chat, mobile conversation sheet, search, processing state, rename persistence, download, deletion, streamed tokens, citations, conversation switching/new conversation, exports, focus visibility, touch-visible menus, long titles, overflow, and exactly one typing indicator. Use disposable data only.
10. With existing credentials, run bounded upload -> processing -> ready, Supabase storage/database cleanup, Gemini streaming/citations, and genuine Upstash 429 plus post-reset recovery. Inspect redacted logs. Clean all disposable users, records, storage objects, and Redis keys, and verify cleanup directly. If credentials or browser dependencies are unavailable, report that as not verified rather than simulating success. Do not run production tests unless explicitly authorized.
11. Report exactly: Changed; Commands run; Dependency and install-script findings; Type/lint/test/build evidence; Vitest findings; Dev-runtime findings; Visual QA; Credential-backed runtime findings; Cleanup; Not verified; Risks; Rollback; Next step. Distinguish compilation, passing assertions, browser verification, live-provider verification, and production verification.
