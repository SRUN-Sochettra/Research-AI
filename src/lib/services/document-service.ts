/**
 * Document Service
 * Handles high-level document operations: upload, process, delete.
 */
export class DocumentService {
  static async uploadAndProcess(file: File) {
    // 1. Upload to storage
    // 2. Parse PDF
    // 3. Chunk text
    // 4. Generate embeddings
    // 5. Store in DB
  }
}
